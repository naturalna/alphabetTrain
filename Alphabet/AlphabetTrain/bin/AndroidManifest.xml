<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.startproject.alphabettrain"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="18" />
    
    <uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="my.startproject.alphabettrain.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
			android:name=".LearnActivity"
			android:label="@string/learn_activiti_title" >
		</activity>
		<activity
			android:name=".TestsActivity"
			android:label="@string/tests_activiti_title" >
		</activity>
		<activity
			android:name=".ProgressActivity"
			android:label="@string/progress_activiti_title" >
		</activity>
		<activity
			android:name=".HomeActivity"
			android:label="@string/home_activiti_title" >
		</activity>
		<activity
			android:name=".RegisterActivity"
			android:label="@string/home_activiti_title" >
		</activity>
		<activity
			android:name=".SigninActivity"
			android:label="@string/home_activiti_title" >
		</activity>
		<activity
			android:name=".RessetPasswordActivity"
			android:label="@string/home_activiti_title" >
		</activity>
        <service
           android:name=".PlayAudio"  android:enabled="true"
             >
        </service>
    </application>

</manifest>
